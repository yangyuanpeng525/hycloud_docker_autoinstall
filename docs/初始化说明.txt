+++++++++++++++++++++++++++++++++++++++++++++++++
#用户需要关心的文件
1、主目录下的inventory------需要用户自行上传，有参考模板inventory.temp
2、主目录下的var------ansible安装执行目录、下载地址
3、主目录下的latest_version.txt------最新版本号，默认安装最新版本，若要自定义，修改docs/install_version.txt（程序执行后自动生成,内容为: 本次安装的所有应用:版本号 ），也可以copy模板文件install_version.txt.temp再进行修改
